{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/constantin-jais/rumble-ai-clearance/schemas/governance.schema.json",
  "title": "rumble-ai-clearance curated governance data",
  "description": "Curated model-creator governance data shipped in content/governance/providers.yaml: country of origin per provider, with sources. Public and reviewable; corrections by PR.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "providers"],
  "properties": {
    "version": { "const": 1 },
    "providers": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "name", "origin", "source"],
        "properties": {
          "id": {
            "type": "string",
            "description": "Canonical provider id used across the product, kebab-case."
          },
          "name": { "type": "string" },
          "origin": {
            "type": "string",
            "pattern": "^[A-Z]{2}$",
            "description": "ISO 3166-1 alpha-2 country of the company's headquarters."
          },
          "source": {
            "type": "string",
            "description": "Citation backing the entry (official company page)."
          },
          "aliases": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Hugging Face organisation handles mapped onto this provider by the sync pipeline."
          }
        }
      }
    }
  }
}
