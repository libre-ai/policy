//! `clearance` — sync, validate, evaluate, explain and gate (`check`) AI model
//! eligibility against a versioned policy. Fail-closed everywhere.
#![forbid(unsafe_code)]

fn main() {
    // Commands land with their phases: sync | validate | evaluate | explain | check.
}
