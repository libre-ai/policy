//! Dioxus web UI, dual mode: local (WASM engine, files never leave the
//! browser — public demo mode) and server (client of the org's API).
#![forbid(unsafe_code)]

fn main() {
    // UI lands with phase 9.
}
