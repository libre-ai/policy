{
  "name": "@libre-ai/policy-gates",
  "private": true,
  "license": "EUPL-1.2",
  "type": "module",
  "exports": {
    ".": "./packages/policy-core-ref/src/index.ts"
  },
  "types": "./packages/policy-core-ref/src/index.ts",
  "packageManager": "bun@1.4.0-canary.1",
  "engines": {
    "bun": ">=1.4.0"
  },
  "overrides": {
    "fast-uri": "4.1.4"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "@bytecodealliance/jco-transpile": "0.4.2",
    "@libre-ai/contracts-authority": "github:libre-ai/contracts#ee144f03cf7d3deeda02a9fcd5e10377c2e99cb4",
    "@libre-ai/governance": "github:libre-ai/governance#8c238c09efd443a4866459160dac1cfee31d88be",
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  },
  "scripts": {
    "check:bun:runtime": "bun node_modules/@libre-ai/governance/tools/quality/check-bun-minimum.ts",
    "check:bun": "bun run check:bun:runtime && bun node_modules/@libre-ai/governance/tools/quality/check-bun-manifests.ts",
    "check:toolchain": "bun run check:bun && bun node_modules/@libre-ai/governance/tools/quality/check-toolchain.ts",
    "check:wit": "bun run check:bun:runtime && bun scripts/check-vendored-wit.ts",
    "check:wasm-conformance": "bun run check:bun:runtime && node tools/quality/build-policy-core-wasm.ts && cargo run --locked --example check_wasm_imports -- target/wasm32-unknown-unknown/release/policy_core.wasm && bun tools/quality/policy-core-wasm-conformance.ts",
    "check:app": "bun run check:bun:runtime && bun install --cwd packages/policy-core-ref && bun test --cwd packages/policy-core-ref && bun install --cwd apps/model-policy && bun test --cwd apps/model-policy",
    "check:secret-scan": "bun run check:bun:runtime && bun node_modules/@libre-ai/governance/tools/quality/check-secret-scan.ts",
    "check:personal-data": "bun run check:bun:runtime && bun node_modules/@libre-ai/governance/tools/quality/check-personal-data-boundary.ts",
    "lint": "bun run check:bun:runtime && biome ci .",
    "pretest": "bun run check:bun",
    "test": "cargo test --locked",
    "check": "bun run check:bun && bun run check:toolchain && bun run check:wit && bun run check:app && bun run check:secret-scan && bun run check:personal-data && bun run lint"
  }
}
