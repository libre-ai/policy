{
  "name": "rumble-ai-clearance-e2e",
  "private": true,
  "description": "Playwright e2e harness for the rumble-ai-clearance web UI (build with `dx build --platform web --release` first)",
  "scripts": {
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0"
  }
}
